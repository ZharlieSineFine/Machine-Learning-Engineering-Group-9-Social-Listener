# NOTE: The canonical smoke-test requirements live at
#       infra/docker/smoke/requirements.txt — that's what the `smoke`
#       compose service installs.
#
# This file is kept only for IDE convenience when running pytest directly
# from a local venv (without Docker). Keep it loosely aligned with
# infra/docker/smoke/requirements.txt; if they drift, infra/docker/smoke
# wins.
pandas>=2.2
scikit-learn>=1.4
fastapi>=0.110
pydantic>=2.6
httpx>=0.27
pytest>=7.4

# --- Integration test deps (need the compose stack running) ---
psycopg2-binary>=2.9
boto3>=1.34
requests>=2.31
sqlalchemy>=2.0
great-expectations>=0.18,<1
mlflow>=2.10,<3
protobuf>=4.21,<5
evidently>=0.4,<0.5
langdetect>=1.0
