# Local raw dataset paths (copy to .env and adjust if your layout differs).
# The run_daily driver also auto-discovers the workspace folders when these are unset.

YELP_TAR_PATH=../../Yelp_JSON/yelp_dataset/yelp_dataset
TRIPADVISOR_CSV_PATH=../../TripAdvisor_data_cleaned.csv/TripAdvisor_data_cleaned.csv

# Alternative Yelp layout (extracted JSON lines instead of tar):
# YELP_REVIEWS_PATH=../../Yelp_JSON/yelp_academic_dataset_review.json
# YELP_BUSINESS_PATH=../../Yelp_JSON/yelp_academic_dataset_business.json
